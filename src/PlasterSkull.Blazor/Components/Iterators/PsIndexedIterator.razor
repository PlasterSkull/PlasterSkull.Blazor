@namespace PlasterSkull.Blazor

@inherits ComponentBase

@typeparam T

<PsCondition Evaluation="@_canIterateItems">
    @foreach (PsIndexedIteratorItem<T> item in _items)
    {
        @ChildContent!.Invoke(item)
    }
</PsCondition>